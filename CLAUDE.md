# 项目代码工程规范

为了保证整个项目的代码质量、可读性和长期的可维护性，在接下来的开发中，请严格遵守以下【代码工程规范】，拒绝编写"屎山"代码：

## 1. 架构与组件拆分规范（拒绝万能文件）
- 单个 .vue 文件的代码行数原则上控制在 200 行以内。如果超过，请主动进行组件拆分。
- 严格遵循【逻辑与视图分离】原则：
  - 将 Excel 解析、数据清洗、日期转换、金额格式化等纯业务逻辑，全部抽离到 `src/utils/` 或 `src/composables/`（组合式函数）中，并编写单元测试/边界测试。
  - Vue 组件只负责接收数据、渲染 UI 和处理用户交互，不把大段的清洗正则写在组件内部。

## 2. 代码整洁与可读性（Clean Code）
- 语义化命名：变量、函数、组件命名必须清晰、见名知意（例如使用 parseExcelRawData 代替 handleData）。
- 杜绝"魔法数字"：所有的状态常量（如：1=规划中，2=实施中）必须定义为 TypeScript Interface / 枚举或统一的常量对象（Constants），并在代码中使用常量引用。
- 关键逻辑必须附带简明扼要的中文注释，解释"为什么要这样做"，而不是"这段代码做了什么"。

## 3. 组件化与复用（UI Level）
- 页面中的"指标卡（KPI Card）"、"数据表格（Data Table）"、"图表容器（ChartWrapper）"必须抽离为独立的子组件。
- 统一使用 Tailwind CSS 进行样式编写，避免在组件底部写大量冗余的自定义 <style>。公共间距、颜色主题请遵循 Tailwind 默认规范。

## 4. 健壮性与异常处理（Robustness）
- 任何涉及用户上传（Excel解析）和数据转换的地方，必须包裹 `try-catch` 块。
- 当遇到解析错误或空数据时，必须有优雅的错误提示（Error Boundary / Toast）和空白页状态（Empty State），绝不允许页面直接崩溃或卡死。

## 5. 测试规范
- 核心业务逻辑（如数据解析、格式化、转换）必须编写单元测试
- 复杂组件需要编写组件测试
- 新增功能必须附带相应的测试用例

请确认你已完全理解上述规范，在接下来的代码生成中，如果发现有违反上述原则的地方，请主动重构。